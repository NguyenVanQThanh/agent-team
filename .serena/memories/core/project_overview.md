# Agent-Team Project Overview

This repository is a multi-agent coding team built around a Claude leader and
external Codex, DeepSeek/CodeWhale, Gemini, and Claude CLI workers.

Read these operational memories as needed:

- `mem:core/agent_workflow`
- `mem:core/code_navigation`
- `mem:core/build_and_test`

The durable team vault is `.claude/memory/`. Serena memories are only the
project-local code and workflow context layer.
