# T-006 Cross-Component Review Plan

## Goal
Verify the Tic Tac Toe and Rock Paper Scissors components against their feature acceptance and confirm Rock Paper Scissors still matches the A-001 component contract.

## Files to create/modify
- Read-only review: `src/games/TicTacToe.jsx`
- Read-only review: `src/games/RockPaperScissors.jsx`
- Read-only review: `.claude/memory/features/F-001-tic-tac-toe.md`
- Read-only review: `.claude/memory/features/F-002-rock-paper-scissors.md`
- Read-only review: `.claude/memory/architecture/A-001-rock-paper-scissors-component.md`
- Created: `.claude/team/plans/T-006.md`
- Created/updated: `.claude/team/status/dev2.env`

## Public interface sketch
- `TicTacToe.jsx` default-exports `function TicTacToe()` with no props.
- `RockPaperScissors.jsx` default-exports `function RockPaperScissors()` with no props.
- Both components are self-contained React function components using top-level hooks only.

## Review findings
- `TicTacToe.jsx` meets F-001 acceptance: default export, 3x3 board, X/O alternation, all 8 winning lines, draw detection, status text, and restart resetting board plus next player.
- `RockPaperScissors.jsx` meets F-002 acceptance: default export, three choice buttons, random computer choice over rock/paper/scissors, matchup-table result resolution, persistent player/computer/draw scores, last-round display, and restart resetting scores plus current round.
- `RockPaperScissors.jsx` matches A-001: local `useState`, no external dependencies beyond React, `Math.random()` computer pick, expected matchup table, three choice buttons, score counters, last picks/result, and restart clearing scores and last round.
- No obvious React hook-order or stale-state bugs found in either component.
- No `package.json` exists, so no project lint/test command was available; verification was source re-read and JSX sanity review.

## Sub-tasks in dependency order
- None. No deviations found and no backlog follow-up is recommended.

## Open questions
- None for this review.
