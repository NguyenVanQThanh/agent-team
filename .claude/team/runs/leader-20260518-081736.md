# Leader run 20260518-081736

## Request
Convert the two existing React JSX games (`src/games/TicTacToe.jsx`, `src/games/RockPaperScissors.jsx`) into standalone HTML files (React 18 + Babel standalone via CDN, no build step, double-click to play). Then build `src/games/dashboard.html` — a landing page with two clickable cards that switch between the games. All three files must live together in `src/games/` and work via `file://`.

## Vault context
- [[features/F-001-tic-tac-toe]] — shipped JSX source for T-001; preserves canonical game rules.
- [[features/F-002-rock-paper-scissors]] — shipped JSX source for T-002.
- [[architecture/A-001-rock-paper-scissors-component]] — implementation contract for RPS; the HTML port must keep the same semantics.
- Backlog BL-02 — deepseek (`exec --auto`) now has `write_file` + `exec_shell`, so dev4 is back online for M-sized file work. This run will be the first real validation.

## Plan
| task  | size | assignee | summary |
|-------|------|----------|---------|
| T-001 | M    | dev1 (codex)    | Standalone TicTacToe.html (React 18 + Babel via CDN) |
| T-002 | M    | dev4 (deepseek) | Standalone RockPaperScissors.html (same approach) |
| T-003 | L    | dev5 (opus)     | dashboard.html — landing page with 2 cards switching between the games |

Why this routing:
- dev1 + dev4 are the M-bracket coders (codex + deepseek). Splitting T-001 and T-002 between them lets all three CLIs run in parallel.
- T-003 is L (multi-component UX + needs to wire to the other two outputs) → dev5 (opus, senior all-rounder, L bracket).
- dev2 is a planner who refused prod edits last run; not used here.
- Three distinct CLIs (codex, deepseek, opus) gives maximum parallelism and tests BL-02's fix.

T-003 declares `depends_on: T-001,T-002` but the dashboard only needs the *filenames* (`TicTacToe.html`, `RockPaperScissors.html`) which are fixed by spec — so it can safely run in parallel.

## Spawn command
```
.claude/bin/spawn-team.sh dev1:codex:T-001 dev4:deepseek:T-002 dev5:opus:T-003
```

## Devs status (after wait)
- dev1 (codex)    — done — `src/games/TicTacToe.html`; "verified CDN/createRoot/status/win/draw/restart via static smoke check".
- dev4 (deepseek) — done — `src/games/RockPaperScissors.html`; all JSX logic preserved; mounts via createRoot. **First successful deepseek FS write since BL-02 fix.**
- dev5 (opus)     — done — `src/games/dashboard.html`; header + 2 cards, iframe embed, Back button + Esc.

spawn-team.sh exit: 0 / 3 failures.

## Verification
- T-001 ✓ — re-read confirms React 18 + Babel from unpkg, all 8 winning lines, useMemo/useState as in source JSX, ReactDOM.createRoot at line 234. Clean polished `<style>` (responsive board, focus-visible outlines).
- T-002 ✓ — re-read confirms React 18 prod build + Babel from unpkg, CHOICES/WINNING_MATCHUPS/INITIAL_SCORES/getRoundResult identical to JSX, scoreboard + last-round + Restart, createRoot at line 213.
- T-003 ✓ — re-read confirms header "🎮 Game Dashboard", 2 cards with `data-src="TicTacToe.html"` and `data-src="RockPaperScissors.html"` (sibling-relative — works on file://), iframe mounting in `#gameFrame`, Back button unsets iframe src to `about:blank` (fully unmounts the embedded React app), Esc key support, responsive grid with `auto-fit minmax(260px, 1fr)`.
- Directory listing confirms all 3 HTML files coexist in `src/games/` alongside the original JSX sources.

No re-routing, no follow-ups filed.

## Memory updates
- updated [[features/F-001-tic-tac-toe]] — added HTML port to Changelog; updated `related` to include T-001-r2.
- updated [[features/F-002-rock-paper-scissors]] — added HTML port to Changelog; updated `related` to include T-002-r2.
- created [[features/F-003-game-dashboard]] — status=shipped, owner=dev5, links to F-001 + F-002.
- updated [[features/_moc]] — appended F-003.
- updated [[_index]] — appended one-line entry for this run.
