# Leader run 20260520-084806

## Request
Fix two browser games:
- T-001 FruitBox: (1) fix broken drag-select rubberband, (2) bump CELL 38→50,
  (3) reduce GAME_SECONDS 120→60.
- T-002 Minesweeper: add a score system (+10 per safe reveal, +50 per
  correctly-flagged mine at win, +max(0,500-3*timer) win bonus, reset on new
  game, surface in HUD + both overlays).

Auto mode active. Suggested assignment: dev8 (sonnet, L) on T-001,
dev9 (sonnet, L) on T-002. Both tasks are file-independent — no conflicts.

## Vault context
- [[features/F-001-browser-games-dashboard]] — these are patches to the games
  shipped in run 20260520-033403.
- [[../team/tasks#Backlog]] T-FU01 — sub-CLIs were blocked by harness write
  perms last run; leader had to self-handle all 6 files. Still open.

## Plan
| task  | size | assignee | summary |
|-------|------|----------|---------|
| T-001 | M    | dev8 (sonnet) | FruitBox: drag-select fix + CELL 50 + GAME_SECONDS 60 |
| T-002 | M    | dev9 (sonnet) | Minesweeper: score system + HUD + overlays |

Both M-sized, file-disjoint, safe to run in parallel.

## Spawn command
```
.claude/bin/spawn-team.sh dev8:sonnet:T-001 dev9:sonnet:T-002
```

## Devs status (after wait)
- dev8 (sonnet): done — files: games/fruitbox.html, games/js/fruitbox.js.
  Root cause was a buildDOM() innerHTML wipe destroying the #rubberband div
  on init; updateRubberband() then crashed on null at first mousedown,
  silently aborting every drag. Fix: append a fresh rubberband at end of
  buildDOM. Also CELL 38→50, .apple 38px→50px, GAME_SECONDS 120→60.
- dev9 (sonnet): done — files: games/minesweeper.html, games/js/minesweeper.js.
  Added state.score, +10 in floodReveal, +50*correctFlags computed before
  auto-flag sweep, +max(0,500-3*timer) win bonus, #score-display in HUD
  (with star separator, 4-digit zero-padded), #overlay-score on both
  win/lose overlays, reset in newGame, updateScoreUI() called from render
  so live updates work.

(Note: both sub-CLI processes reported pid-exit=1 but produced correct
status files and actually wrote source. Spawn script also flagged
`is_tournament: unbound variable` in two places — harmless but worth
filing as a small follow-up.)

## Verification
- T-001: passed.
  - CELL=50 in JS (fruitbox.js:21), GAME_SECONDS=60 (fruitbox.js:22).
  - .apple width/height = 50px in CSS (fruitbox.html:130-131).
  - #rubberband re-created at end of buildDOM (fruitbox.js:68-72).
  - mousedown on board, mousemove/mouseup on window (works outside board).
  - preventDefault on down/move/up; boardLocal uses getBoundingClientRect.
  - Leader polish: timer placeholder 2:00→1:00; removed stale HTML
    #rubberband div (now created in buildDOM only).
- T-002: passed.
  - state.score field added; reset to 0 in newGame (minesweeper.js:344).
  - +10 per revealed cell in floodReveal (minesweeper.js:145) — applies to
    both single reveals and BFS cascade.
  - +50 per correctly-flagged mine counted **before** auto-flag sweep
    (minesweeper.js:215-222) so only player-placed flags count.
  - Win bonus +max(0,500-3*timer) (minesweeper.js:224).
  - #score-display in HUD (minesweeper.html:192); updateScoreUI called
    from render (minesweeper.js:402) → live update on every reveal/flag.
  - #overlay-score updated in showOverlay (minesweeper.js:305) on both
    win and lose paths.

## Memory updates
- features/F-001-browser-games-dashboard.md → append a "Patches 2026-05-20"
  section noting the drag-select root cause (innerHTML wipe destroying
  rubberband) and the minesweeper scoring extension.
- _index.md → append one-line summary of this run.
- T-FU01 (harness perms) note: this run shows sub-CLIs CAN now write files
  in the environment (both dev8 and dev9 wrote .html and .js successfully)
  even though `pid-exit=1`. Worth investigating whether T-FU01 is partially
  resolved.

## Follow-ups
- T-FU02 (proposed): spawn-team.sh emits `is_tournament: unbound variable`
  at lines 423 and 634 — harmless under `set -u` for now but should be
  initialized to `false` at top of script.
- T-FU03 (proposed): both sub-CLIs exited 1 despite writing correct
  status=done files; root cause unknown — possibly Sonnet CLI exits 1
  when its agent loop hits the turn cap even on successful completion.
  Doesn't block the team but pollutes "failures or blockers" math.
